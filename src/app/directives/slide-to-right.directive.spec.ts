import { SlideToRightDirective } from './slide-to-right.directive';

describe('SlideToRightDirective', () => {
  it('should create an instance', () => {
    const directive = new SlideToRightDirective();
    expect(directive).toBeTruthy();
  });
});
