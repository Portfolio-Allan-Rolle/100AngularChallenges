import { ArraySufflePipe } from './array-suffle.pipe';

describe('ArraySufflePipe', () => {
  it('create an instance', () => {
    const pipe = new ArraySufflePipe();
    expect(pipe).toBeTruthy();
  });
});
